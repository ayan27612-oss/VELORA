const people=[{name:'Mira',age:24,intent:'serious',city:'Mumbai',bio:'Coffee, long walks and conversations that accidentally become 3 hours long.',chips:['Books','Design','Travel'],initial:'M'},{name:'Arjun',age:27,intent:'serious',city:'Pune',bio:'Building things, cooking badly, and looking for something real.',chips:['Tech','Food','Fitness'],initial:'A',two:true},{name:'Rhea',age:25,intent:'casual',city:'Thane',bio:'Music, tiny cafes and zero interest in playing guessing games.',chips:['Music','Art','Cafes'],initial:'R',three:true}];
const cards=document.querySelector('#cards');let filter='all';
function render(){cards.innerHTML=people.filter(p=>filter==='all'||p.intent===filter).map(p=>`<article class="card"><div class="avatar ${p.two?'two':''} ${p.three?'three':''}"><div class="initial">${p.initial}</div></div><div class="card-body"><div class="card-top"><h3>${p.name}, ${p.age}</h3><span class="badge">${p.city}</span></div><p>${p.bio}</p><div class="chips">${p.chips.map(c=>`<span class="chip">${c}</span>`).join('')}</div><div class="card-actions"><button class="pass" data-name="${p.name}" data-action="pass">Pass</button><button class="like" data-name="${p.name}" data-action="like">Like</button></div></div></article>`).join('')||'<p>No profiles in this filter yet. The app is still being built, because apparently people need to exist before they can date.</p>'}
render();document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()});
document.addEventListener('click',e=>{if(e.target.dataset.action)toast(e.target.dataset.action==='like'?`Like sent to ${e.target.dataset.name}.`:`${e.target.dataset.name} skipped.`)});
const modal=document.querySelector('#modal');document.querySelector('#profileBtn').onclick=()=>modal.classList.remove('hidden');document.querySelector('#startBtn').onclick=()=>document.querySelector('#discover').scrollIntoView();document.querySelector('#aboutBtn').onclick=()=>document.querySelector('.values').scrollIntoView();document.querySelector('#waitBtn').onclick=()=>toast('Early-list flow queued for the next build.');document.querySelector('#closeModal').onclick=()=>modal.classList.add('hidden');
document.querySelector('#profileForm').onsubmit=e=>{e.preventDefault();localStorage.setItem('velora-profile',JSON.stringify({name:name.value,age:age.value,intent:intent.value,bio:bio.value}));modal.classList.add('hidden');toast('Profile saved locally. No database involved.');};
function toast(t){const x=document.querySelector('#toast');x.textContent=t;x.classList.add('show');clearTimeout(window.tt);window.tt=setTimeout(()=>x.classList.remove('show'),2200)}